import { BorderRadius, Padding } from '../../util';
export declare const CardDiv: import("styled-components").StyledComponent<"div", import("styled-components").DefaultTheme, {
    padding?: Padding[];
    boxShadow: boolean;
    borderRadius?: BorderRadius;
    hasBorder: boolean;
}, never>;
