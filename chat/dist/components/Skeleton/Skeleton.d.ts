import { SkeletonProps } from './interfaces';
declare const Skeleton: ({ children, variant, borderRadius, height, width, }: SkeletonProps) => import("react/jsx-runtime").JSX.Element;
export default Skeleton;
