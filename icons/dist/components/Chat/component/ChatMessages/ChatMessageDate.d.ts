import { ChatProps } from '../interfaces';
import { ChatMessage } from './interfaces';
export declare const ChatMessageDate: ({ date, from, onColor, language, }: ChatMessage & ChatProps) => import("react/jsx-runtime").JSX.Element;
