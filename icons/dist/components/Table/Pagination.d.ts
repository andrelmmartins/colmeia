import { TablePagination } from './interfaces';
declare const Pagination: ({ handleChange, items, pages, page, total, itemsLabel, itemsPerPageLabel, ofPageLabel, pageLabel, }: TablePagination) => import("react/jsx-runtime").JSX.Element;
export default Pagination;
